[package]
name = "nfswolf-nfs3"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "NFS version 3 (RFC 1813): all 22 procedures, the MOUNT v3 protocol, and domain types over the raw wire format."
keywords = ["nfs", "nfsv3", "rfc1813", "mount", "onc-rpc"]
categories = ["network-programming", "filesystem", "encoding"]
readme = "README.md"

[dependencies]
nfswolf-xdr = { workspace = true }
nfswolf-rpc = { workspace = true }
thiserror = "2"
tracing = "0.1"

[lints]
workspace = true
